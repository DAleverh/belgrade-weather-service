version: '3.8'

services:
  weather-api:
    build: .
    container_name: belgrade-weather-service
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
